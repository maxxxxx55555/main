# Базовые зависимости ($0-стек): aiosqlite (SQLite) + openai SDK (совместим с Groq/OpenRouter)
aiogram>=3.13,<4
sqlalchemy[asyncio]>=2.0,<3
aiosqlite>=0.20
pydantic-settings>=2.4
openai>=1.40
alembic>=1.13

# Векторный поиск (локальный, данные в ./chroma_db — без внешних сервисов)
chromadb>=0.5

# --- Опционально (не нужны для $0-стека) ---
# asyncpg>=0.29        # PostgreSQL (если переедете с SQLite)
# redis>=5.0           # Redis FSM/throttle (MVP работает без него — in-memory)