# ⚡ QUICK START — запуск за 5 минут (без Docker)

От нуля до работающего бота на вашей машине. Всё проверено: тесты 33/33 ✅

---

## Шаг 1. Токен от @BotFather (2 минуты)

1. Откройте Telegram, найдите **@BotFather**
2. Отправьте `/newbot`
3. Введите **имя** бота (например, `AI Сотрудник`) — видят пользователи
4. Введите **username** (например, `my_ai_employee_bot`) — должен заканчиваться на `bot`
5. BotFather пришлёт токен вида:
   ```
   7123456789:AAHfk3n2l1xXyZ0example_token_here123
   ```
6. **Скопируйте токен** — он понадобится в шаге 3. Никому не показывайте!

> 💡 Рекомендую сразу: `/setdescription` и `/setabouttext` — готовые тексты в
> [`docs/LAUNCH_MATERIALS.md`](docs/LAUNCH_MATERIALS.md). Также `/setuserpic` — загрузите аватар.

---

## Шаг 2. Установка проекта (3 минуты)

Нужен Python 3.11+ (`python --version`).

```bash
cd путь/к/проекту

# 1) Виртуальное окружение
python -m venv .venv

# Windows:
.venv\Scripts\pip install -e .[dev]
# Linux/macOS:
.venv/bin/pip install -e ".[dev]"

# 2) Шаблон конфигурации
copy .env.example .env      # Windows
cp .env.example .env        # Linux/macOS
```

---

## Шаг 3. Настройка .env

Откройте `.env` любым редактором и заполните:

```env
# ОБЯЗАТЕЛЬНО — токен из шага 1:
BOT_TOKEN=7123456789:AAHfk3n2l1xXyZ0example_token_here123

# НЕ ОБЯЗАТЕЛЬНО — без ключа бот работает в MOCK-режиме (см. ниже):
LLM_API_KEY=

# Не трогайте пока: SQLite-файл создастся сам
DATABASE_URL=sqlite+aiosqlite:///./data/bot.db

# Ваш Telegram ID (для админ-команд /adminstats, /refund):
# узнать свой ID: напишите @userinfobot
ADMIN_IDS=123456789
```

---

## Шаг 4. Запуск

```bash
# Windows:
.venv\Scripts\python -m app.main
# Linux/macOS:
.venv/bin/python -m app.main
```

Вы увидите:
```
Запуск AI-Сотрудника (mock_llm=True)
Бот запущен (long polling). Ctrl+C для остановки.
```

> ⚠️ `mock_llm=True` — это нормально, если не указан `LLM_API_KEY`. Бот работает,
> но отвечает заглушками. Хотите настоящие AI-ответы — см. «Mock-режим» ниже.

---

## Шаг 5. Тест в Telegram

Откройте чат со своим ботом и проверьте:

| Команда | Ожидаемый результат |
|---|---|
| `/start` | Приветствие + меню: «⭐️ Тарифы», «📚 База знаний», «ℹ️ Помощь» |
| Напишите текст, например `Привет, кто ты?` | Ответ от AI-Сотрудника + кнопки меню |
| `/help` | Справка по всем командам |
| `/stats` | Ваш тариф Free, лимит 0/30, дата обновления |
| `/buy` | Список тарифов (Pro 100 ⭐️, Business 250 ⭐️) — оплата реальна через Stars |
| Кнопка «📚 База знаний» | Предложение подключить PRO (на Free) |
| `/forget_me` → «Да» | Полное удаление ваших данных (проверка приватности) |

> 💰 Оплата Stars работает по-настоящему: для теста платежа пополните себе Stars
> (~$2) и купите Pro — лимит вырастет до 500 сразу.

---

## 🎭 Как работает Mock-режим (без LLM-ключа)

Когда `LLM_API_KEY` пуст, включается `MockProvider`:

- Отвечает **детерминированно**: `[MOCK] Принял запрос: «…»` + описание функций
- Никаких внешних запросов — сеть не нужна, расходы 0 ₽
- Идеален для: разработки UI, тестов (их 33 — все на mock), CI, демо клиентам
- Включить принудительно (даже с ключом): `LLM_MOCK=1` в `.env`

**Mock-ответ выглядит так:**
```
[MOCK] Принял запрос: «Привет, кто ты?». Как AI-Сотрудник: квалифицирую лид,
отвечу на вопрос клиента или запишу на консультацию. Подключите LLM_API_KEY
для реальных ответов.
```

---

## 🔑 Подключение реального AI (2 минуты)

Вариант А — **GLM (дёшево, есть бесплатная квота)**:
1. Зарегистрируйтесь на https://open.bigmodel.cn → API Keys
2. В `.env`:
   ```env
   LLM_API_KEY=ваш_ключ_zhipu
   LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
   LLM_MODEL=glm-4.5
   ```

Вариант Б — **OpenAI**:
```env
LLM_API_KEY=sk-...
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
```

Перезапустите бота — в логе должно быть `mock_llm=False`. Ответы стали настоящими.

---

## 🧪 Прогон тестов (проверка здоровья проекта)

```bash
.venv\Scripts\pytest -q          # Windows
.venv/bin/pytest -q              # Linux/macOS
```
Ожидается: `33 passed`. Тесты не требуют ни токена, ни ключей, ни сети.

---

## ❓ Проблемы

| Симптом | Решение |
|---|---|
| `BOT_TOKEN не задан` | Вы скопировали `.env.example` в `.env`? Токен вписан? |
| `Conflict: terminated by other getUpdates` | Бот запущен дважды — убейте второй процесс |
| `module named 'aiogram'` | Не активировали venv / ставили не в него — шаг 2 |
| Бот молчит | Проверьте токен; запустите `/start` именно у вашего бота |
| Ошибка БД `data/` | Каталог создастся сам; проверьте права на запись |

Дальше: прод-деплой на VPS за €5 — см. [`DEPLOY.md`](DEPLOY.md).
